{% extends 'base.html.twig' %}

{% block title %}Extras{% endblock %}

{% block body %}
    <h1>Extras</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ extra.id }}</td>
            </tr>
            <tr>
                <th>TypeExtras</th>
                <td>{{ extra.typeExtras }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('extras_index') }}">back to list</a>

    <a href="{{ path('extras_edit', {'id': extra.id}) }}">edit</a>

    {{ include('extras/_delete_form.html.twig') }}
{% endblock %}
